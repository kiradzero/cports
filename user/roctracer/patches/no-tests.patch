Tests use the legacy FindHIP module and require amdgpu hardware;
don't build them. Also drop -Werror.

diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -43,6 +43,6 @@
 set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
-add_compile_options(-Wall -Wno-error=ignored-attributes -Werror)
+add_compile_options(-Wall -Wno-error=ignored-attributes)
 # To set addition RUNPATH in libraries
 # installed in /opt/rocm-ver/lib/roctracer
 set(ROCM_APPEND_PRIVLIB_RPATH "$ORIGIN/..")
@@ -60,10 +60,5 @@
 ## Build libraries
 add_subdirectory(src)
 
-## Build tests
-if(${LIBRARY_TYPE} STREQUAL SHARED)
-  add_subdirectory(test)
-endif()
-
 ## Build Plugins
 add_subdirectory(plugin)
